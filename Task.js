// Create a function that takes a number as its only argument and returns true if
// it's less than or equal to zero, otherwise return false
function morning(a){
    if(a <= 0){
        return true
    }
    else{
        return false
    }
}
console.log(morning(2))